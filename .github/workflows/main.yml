name: Download Files
permissions: write-all


on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Run cmd
      run: |
        sudo wget https://github.com/anomalyco/opencode/releases/download/v1.14.39/opencode-desktop-win-arm64.exe

    - name: Commit and push files
      uses: EndBug/add-and-commit@v7
      with:
        author_name: "majidzahedi"
        author_email: "majidzahedi@github.com"
        message: "Upload Files"
        add: "."
        push: "https://github.com/majidzahedi/dinasor.git"
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
